function Asd() {
    const ewq = [321, 432, 543, 654, 765];
    const tre = (ytr: number, uyt: number) => {
    const poi = ewq.map(
        (iuy) => (iuy === ytr ? uyt : iuy));
        console.log(poi);
    };

    return (
        <div>
            <button onClick={() => tre(543, 999)}>
                BUTTON
            </button>
        </div>
 );
}
export default Asd;